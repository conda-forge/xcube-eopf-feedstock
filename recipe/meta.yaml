{% set name = "xcube-eopf" %}
{% set version = "0.3.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/EOPF-Sample-Service/xcube-eopf/archive/v{{ version }}.tar.gz
  sha256: 26514c3bfc723d0d405ededc6d088bf4cafc3b56db1bc3b84784291f459ab0bc

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - dask
    - numpy
    - pyproj
    - pystac
    - pystac-client
    - xarray
    - xarray-eopf ==0.1.2
    - xcube >=1.11.0
    - xcube-resampling
test:
  imports:
    - xcube_eopf
  requires:
    - python {{ python_min }}

about:
  summary: A xcube datastore implementation that allows for analysis-ready reading of ESA EOPF data products.
  home: https://github.com/EOPF-Sample-Service/xcube-eopf
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  description: xcube-eopf is a Python package
  dev_url: https://github.com/EOPF-Sample-Service/xcube-eopf

extra:
  recipe-maintainers:
    - pont-us
    - konstntokas
    - forman
